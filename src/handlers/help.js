module.exports = (ctx) => {
  return ctx.reply(
    [
      'Доступные команды:',
      '/start — приветствие',
      '/help — эта справка',
      'Отправь текст — отвечу рифмованной парой строк',
    ].join('\n')
  );
};


